# utils package — shared helpers for meepIntro simulations
# Key modules:
#   utils.sweep     — PhaseLibrary, sweep(), run_unit_cell()
#   utils.materials — load_material()
#   utils.device    — print_platform_info(), get_torch_device()
#   utils.viz       — plotting functions
